// const Validator = require('validator');
// const validText = require('./valid-text');

// module.exports - function validateCommentInput(date){
//     let errors = {};

//     data.number = validText(data.number) ? data.number: '';

//     if (data.number && !Validator.)

// }